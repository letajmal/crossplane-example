apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: crossplane
    repo: https://charts.crossplane.io/stable
    version: 2.3.1 # Define the global chart version here
    releaseName: crossplane
    namespace: crossplane